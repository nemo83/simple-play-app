apiVersion: v1
kind: Service
metadata:
  name: simple-play-app
  labels:
    app: simple-play-app
spec:
  ports:
    - port: 9000
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: simple-play-app
